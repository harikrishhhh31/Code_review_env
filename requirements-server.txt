# Pin gradio>=5.0.0 to avoid websockets conflict with openenv-core 0.2.3
# openenv-core requires websockets>=15.0.1
# gradio 4.x requires websockets<12.0 (conflict!)
# gradio 5.x+ is compatible with websockets>=15.0.1
gradio>=5.0.0
openenv-core>=0.2.3
fastapi>=0.115.0
uvicorn>=0.24.0
